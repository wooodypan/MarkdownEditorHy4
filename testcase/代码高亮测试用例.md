# 代码高亮测试用例

第一批只支持 JavaScript / Python / Swift 三种语言，其它语言（以及没写语言的）按纯文本显示。

## Swift

```swift
import Markdown

// 只有标题层级折叠
let document = Document(parsing: markdown)
for child in document.children {
    print(type(of: child))
}

/* 块注释里写 let 也不该被当成关键字：
   这是一段多行注释 */
final class EditorViewController: UIViewController {
    private static let maximumHeight: CGFloat = 0x1F * 2
    var count: Int = 42
    var ratio: Double = 3.14e-8
    var name = "含有 \"引号\" 的字符串"
    var multi = """
    三引号字符串里的 if / return 都不算关键字
    """
}
```

## JavaScript

```js
// 行注释
const numbers = [1, 2, 3];
let total = 0xFF + 0b1010;
/* 块注释 */
function format(user) {
    return `hello ${user.name}, you have ${numbers.length} items`;
}
class DocumentView extends UIView {
    render() { return true; }
}
```

## Python

```python
# Python 的注释用井号
import os


def greet(name, times=3):
    """三引号（docstring）里的 return 也不该被当成关键字"""
    doc = """
    多行字符串：if x > 0
    """
    prefix = f"hi {name} " 'and'
    for _ in range(times):
        print(prefix, os.sep, 1_000, 0.5e3, 3j)
    return True


class Document:
    def __init__(self):
        self.title = '单引号字符串'
```

## 不支持的语言（应该全是默认色）

```sql
SELECT id, name FROM users WHERE count > 10;
```

## 没写语言的围栏

```
这是一段没有标注语言的代码 let const def 都不该上色
```
