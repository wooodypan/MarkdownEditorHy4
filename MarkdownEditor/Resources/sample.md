# Markdown 编辑器 Demo

这是一个用 swift-markdown 解析 AST、再用 TextKit 2 渲染的编辑器。**显示的是排版效果，复制出来的是源码。**

![示例图片](sample.png)

## 一、行内语法

这里是 **粗体**、*斜体*、***粗斜体***、~~删除线~~、`行内代码`，以及一个 [链接](https://swift.org)。

## 二、无序列表

- 一级列表项
- 一级列表项，带一个嵌套列表
  - 二级列表项
  - 另一个二级列表项
    - 三级列表项
- 回到一级

## 三、有序列表

1. 有序列表第一项
2. 有序列表第二项
3. 有序列表第三项

## 四、引用

> 引用块里换行也一样对齐：
> 这是引用的第二行。

## 五、代码

```swift
let document = Document(parsing: markdown)
for child in document.children {
    print(type(of: child))
}
```

## 六、分隔线

# 表格渲染测试

| 姓名 | 年龄 | 城市 |
| :--- | :--: | ---: |
| 张三 |  18  | 上海 |
| 李四 |  20  | 武汉 |

---

## 七、动手试一下

1. 选中上面那张图片按 `⌘C`，粘到任何地方 —— 拿到的是 `![示例图片](sample.png)` 这段源码
2. 光标放到任意列表项文字的最前面按退格 —— 列表标记被删掉，整项降级成普通段落
